# class_name Animal_Scent

# # var scent_strength : float
# var scent_direction : Vector2i # specifies the tile where the animal went from current tile
# var scent_duration_left : int # every turn this gets decremented by 1, when it reaches 0, the scent is removed
# var animal_id

# func _init(set_scent_direction, set_scent_duration_left, set_animal_id):
#     scent_direction = set_scent_direction
#     scent_duration_left = set_scent_duration_left
#     animal_id = set_animal_id
    